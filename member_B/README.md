# member_B 工作区说明

本文件夹是**成员 B / 成员 2** 的协作工作区（创建收藏、编辑、上传、表单与整合）。

---

## 成员 E 代写的阶段二·任务五（AI 建议面板）

成员 B 尚未开工时，由**成员 E / 成员 5** 预先完成 **AI 建议面板** 与 API 客户端，供成员 B 直接复用或合并。

| 说明 | 路径 |
|---|---|
| 成员 E 交付说明 | `docs/Phase2_Task5_AI_Panel_by_Member_E.md` |
| Flutter 源码（工作区副本） | `frontend/lib/features/collection_form/` |
| 已接入 Demo 的 Add 页挂钩 | 见 `docs/Phase2_Task5_AI_Panel_by_Member_E.md` §「运行中的副本」 |

**运行中的副本**（Flutter 可编译）同步在：

```text
frontend/lib/features/collection_form/
```

成员 B 正式开发时，以本文件夹为归属边界；若与 `frontend/` 副本冲突，以 `member_B/` 内文件为准合并。

---

## 推荐目录（成员 B 完整任务）

见根目录 `Member_2_Create_Upload_Integration_Detail_Plan.md` §三。

当前仅 `collection_form/` 下 AI 相关由成员 E 预先实现；其余 `create_collection_page`、`image_picker_field` 等仍待成员 B 完成。
